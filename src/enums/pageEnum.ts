export enum PageEnum {
  // name
  LOGIN = 'login',
  ROOT = 'root',
  BASE = 'dashboard',
  INVALID = 'not-found',
  REDIRECT = 'redirect',
  NO_AUTH = '403',

  // path
  BASE_HOME = '/dashboard',
}
